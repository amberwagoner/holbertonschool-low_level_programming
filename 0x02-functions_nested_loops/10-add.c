#include "main.h"

/**
 * add - adds two integers and returns the result
 * @x: first number
 * @y: second number
 * Return: 0.
 */

int add(int x, int y)
{
		int result = (x + y);

	return (result);

	return (0);
}
